import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Icons from './Icons'

export default function CardButton() {
    return (
        <View>
            <Icons size={24} icon={'CartIcon'} />
        </View>
    )
}

const styles = StyleSheet.create({})
