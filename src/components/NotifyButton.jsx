import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Icons from './Icons'

export default function NotifyButton() {
    return (
        <View>
            <Icons icon={'BellIcon'} size={24} />
        </View>
    )
}

const styles = StyleSheet.create({})
